[MQTT]
# Address of mqtt server, used to post data
;address = tcp://localhost
;port = 1883

# API Key credentials to authorize communication with the emoncms webserver
;user = <user>
;password = <password>

[HTTP]
# URL of emoncms web server, used to post data
;address = http://localhost/emoncms/

# API Key credentials to authorize communication with the emoncms webserver
;authorization = WRITE
;authentication = <apikey>

# Set the maximum amount of HTTPS requests running asynchronously. Default is 1
;maxThreads = 1

[SQL]
# TCP/IP address of database
;address = 127.0.0.1 
# database port
;port = 3306
# database name
;databaseName = openmuc 

# API Key credentials to authorize communication with the emoncms webserver
;user = <user>
;password = <password>

# TableName Config
;prefix = feed_
# automatically generate table names by feed id
;generic = true
