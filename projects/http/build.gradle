
def projectName = "Emoncms Java - HTTP"

dependencies {
  compile project(':emoncms-api')
  compile group: 'com.google.code.gson', name: 'gson', version: '2.8.5'
}

jar {
  manifest {
    name = projectName
    instruction 'Export-Package', '*'
  }
}
