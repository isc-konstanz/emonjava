dependencies {
  compile project(':emoncms-api')
  compile group: 'com.google.code.gson', name: 'gson', version: '2.8.2'
}

jar {
  manifest {
    name = "Emoncms Java - HTTP"
    instruction 'Export-Package', '*'
  }
}