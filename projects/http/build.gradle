dependencies {
  compile project(':emonjava-api')
  compile group: 'com.google.code.gson', name: 'gson', version: '2.7'
}

jar {
  manifest {
    name = "Emoncms IPC - HTTP"
    instruction 'Export-Package', '*'
  }
}