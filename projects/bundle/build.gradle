
configurations.create('embed')

def osgiversion = '4.3.1'
def openmucversion = '0.17.2'
def gsonversion = '2.7'

repositories {
  flatDir {
    dirs 'dependencies/openmuc', rootDir.getPath() + "/dependencies/openmuc"
  }
}

dependencies {
  compile group: 'org.osgi', name: 'org.osgi.core', version: osgiversion
  compile group: 'org.osgi', name: 'org.osgi.compendium', version: osgiversion
  compile group: 'org.openmuc.framework', name: 'openmuc-core-api', version: openmucversion
  compile group: 'org.openmuc.framework', name: 'openmuc-core-spi', version: openmucversion

  compile project(':emoncms-api')
  embed project(':emoncms-api')

  compile project(':emoncms-http')
  embed project(':emoncms-http')

  compile group: 'com.google.code.gson', name: 'gson', version: gsonversion
  embed group: 'com.google.code.gson', name: 'gson', version: gsonversion
}

jar {
  manifest {
    name = "OpenMUC Data Logger - Emoncms Java"
    instruction 'Bundle-ClassPath', '.,lib/emoncms-api-' + cfgVersion + '.jar,lib/emoncms-http-' + cfgVersion + '.jar,lib/gson-' + gsonversion + '.jar'
    instruction 'Import-Package', '!org.emoncms.*,!com.google.gson.*,*'
    instruction 'Export-Package', ''
  }
}

jar {
  into('lib') {
    from configurations.embed
  }
}